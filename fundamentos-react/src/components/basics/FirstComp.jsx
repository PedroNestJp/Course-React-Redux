export default function FirstComp() {
    const msg = 'first const call on the function'
    return(
        <div>
            <h2>
                First Component
            </h2>
            <p>{msg}</p>
        </div>
    ) 
}